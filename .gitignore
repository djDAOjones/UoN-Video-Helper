# Dependencies
node_modules/

# Build output
dist/
build/
.cache/

# Editor / OS
.DS_Store
*.swp
.vscode/
.idea/

# Local env
.env
.env.local

# Media scratch — never commit source video
/samples/
*.mov
*.mkv

# Dev-only spike fixtures in public/spike/ are transient and must be
# non-sensitive. Real staff media stays outside public/ and is selected through
# the normal app at /?source-picker=file-input in development.
public/spike/
